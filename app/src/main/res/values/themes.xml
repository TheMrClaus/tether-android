<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window shell only; Compose (TetherTheme) owns everything inside.
         windowBackground matches the machine family's mineral to avoid a
         white flash before composition; bars go edge-to-edge from MainActivity. -->
    <style name="Theme.Tether" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#0b0f10</item>
    </style>
</resources>
